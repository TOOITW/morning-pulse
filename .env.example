# Database connection
DATABASE_URL="postgresql://morning_pulse:dev_password_change_in_prod@localhost:5432/morning_pulse_dev?schema=public"

# Email service (choose one)
# Option 1: Gmail SMTP
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"

# Option 2: Resend.com (recommended)
# RESEND_API_KEY="re_xxxxxxxxxx"

# Application
NODE_ENV="development"
APP_URL="http://localhost:3000"

# Newsletter schedule (cron format: minute hour * * *)
# Default: 7:30 AM Taipei time = 23:30 UTC previous day
NEWSLETTER_CRON="30 23 * * *"

# Optional: OpenAI API (for summary enhancement)
# OPENAI_API_KEY="sk-xxxxxxxxxx"
